# 2026-03-04 控制台思考/结果展示错位修复归档

## 背景

在小说章节分析场景中，用户反馈以下问题：

1. 最终结果区域混入了思考过程内容。
2. 思考和结果被拆到两个框时，存在错位展示（仅有单类内容时仍显示双框）。
3. 最终结果区域曾出现思考内容被错误暴露的体验问题。

## 问题定位

本次定位到两处关键链路：

1. 输出拼装层（）
- 历史逻辑会在同时存在 reasoning/text 时拼接“思考过程 + 最终结果”。
- 该行为会放大前端分框解析的风险。

2. 前端分框层（）
- 在仅有  或仅有  的情况下， 判定不精确。
- 导致“只有一种内容却渲染两块容器”的错位现象。

## 修复内容

### 1) 输出层：优先最终结果，思考仅回退展示

文件：

- 为输出增加标准化清洗：
  - 去除 、 标记残留。
  - 解开单层 fenced block（如 ）。
- 输出策略调整为：
  -  存在时，仅输出 。
  -  缺失且  存在时，才输出 。
  - 失败场景输出  + 。

### 2) 展示层：单头部时只展示对应容器

文件：

- 当输入仅包含 ：。
- 当输入仅包含 ：。
- 保证“只有思考只显示思考框；只有结果只显示结果框”。

### 3) 回归测试补充

文件：

新增测试：

1. 
- 断言同时有 reasoning/text 时，展示以  开头。
- 断言不包含 reasoning 明文。

2. 
- 断言  标记会被清理。
- 断言 fenced payload 被正确解包后展示。

## 验证记录

已执行并通过：


[1m[46m RUN [49m[22m [36mv4.0.18 [39m[90m/mnt/c/Users/12281[39m

 [31m❯[39m projects/waoowaoo/tests/unit/helpers/run-stream-state-machine.test.ts [2m([22m[2m0 test[22m[2m)[22m

[2m Test Files [22m [1m[31m1 failed[39m[22m[90m (1)[39m
[2m      Tests [22m [2mno tests[22m
[2m   Start at [22m 22:50:17
[2m   Duration [22m 497ms[2m (transform 213ms, setup 0ms, import 0ms, tests 0ms, environment 0ms)[22m

结果：

- eslint 通过。
- 目标测试文件 8/8 通过。

## 决策说明

1. 本次未调整  中  默认配置。
2. 先通过“输出层约束 + 展示层纠偏 + 单测兜底”完成缺陷修复。
3. 若后续产品策略要求彻底隐藏思考，可再单独提交“默认关闭 reasoning”的变更。

## 影响范围

- 影响模块：运行流状态输出与 LLM 控制台展示。
- 预期收益：
  - 最终结果区域不再混入思考内容。
  - 分框显示逻辑与实际内容一致。
  - provider 返回异常格式时，展示更稳健。
